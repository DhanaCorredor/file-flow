# Normalize line endings to LF in the repository, regardless of the platform
# a contributor works on. Keeps diffs clean across Windows, macOS and Linux.
* text=auto eol=lf

# The git hook is developer tooling, not project source. Language statistics
# should describe what FileFlow is written in, not how it is built.
.githooks/** linguist-detectable=false
